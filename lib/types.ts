export type NavItem = {
  label: string;
  href: string;
};

export type DetailItem = {
  label: string;
  value: string;
};

export type StatItem = {
  label: string;
  value: string;
};

export type SocialLink = {
  platform: string;
  href: string;
};

export type CallToAction = {
  label: string;
  href: string;
};
